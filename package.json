{
  "name": "ri-portal-app",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "start": "node server.js",
    "dev": "NODE_ENV=development node server.js",
    "lint": "node -c server.js",
    "build": "npm ci && npm --prefix client ci && npm --prefix client run build",
    "render-build": "npm ci && npm --prefix client ci && npm --prefix client run build"
  },
  "dependencies": {
    "bcryptjs": "^2.4.3",
    "compression": "^1.7.4",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "jsonwebtoken": "^9.0.2",
    "morgan": "^1.10.0"
  }
}
