# Server-independent configuration (loaded by dotenv in server.js)
# If you don't want secrets in the repo, keep this file only on the server.

# ----- Auth / server -----
# JWT_SECRET=change-me
# PORT=3000

# ----- Shared storage token (required for remote users/stopwords) -----
STORAGE_TOKEN=e7ec8fad891d477d5cf2dcdaa7f0e1cb

# ----- Remote Users (users.json) -----
DEBUG_REMOTE_USERS=1
USERS_REMOTE_URL=http://she0303.hubweb.net/storage/users.php
# You can set this to users.json; the server will still POST to USERS_REMOTE_URL(users.php)
USERS_REMOTE_WRITE_URL=http://she0303.hubweb.net/storage/users.json

# Optional tuning
# REMOTE_USERS_TIMEOUT_MS=3000
# REMOTE_USERS_WRITE_TIMEOUT_MS=5000
# REMOTE_USERS_BACKOFF_MS=30000

# ----- Remote Stopwords (stopwords.json) -----
DEBUG_REMOTE_STOPWORDS=1
STOPWORDS_REMOTE_URL=http://she0303.hubweb.net/storage/stopwords.php
# You can set this to stopwords.json; the server will still POST to STOPWORDS_REMOTE_URL(stopword.php)
STOPWORDS_REMOTE_WRITE_URL=http://she0303.hubweb.net/storage/stopwords.json

# Optional tuning
# REMOTE_STOPWORDS_TIMEOUT_MS=3000
# REMOTE_STOPWORDS_WRITE_TIMEOUT_MS=5000
